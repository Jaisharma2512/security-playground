apiVersion: v1
kind: Service
metadata:
 name: sc-loadbalancer
 namespace: dev
 labels:
   app: securitycheck
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: securitycheck
